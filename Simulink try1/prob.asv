function p = prob(input)
%%need to test
%%need to build a way to implement in large scale using adjacency matrix
%%check what outputting
%%
    delta1=.50;
    delta2=.50;
    

    p(1)=(1-delta1)*past(1) + tau(zeta(past,A))*sus(past,t,prevsus);
    p(1)=(1-delta2)*past(1) + tau(zeta(past,A))*sus(past,t,prevsus);


end